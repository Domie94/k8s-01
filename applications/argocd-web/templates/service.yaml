apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-web.fullname" . }}
  labels:
    app: {{ include "argocd-web.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "argocd-web.name" . }}