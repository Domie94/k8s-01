apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sqlserver-pvc
  labels:
    app: sqlserver
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
  selector:
    matchLabels:
      app: sqlserver
