apiVersion: v1
kind: Service
metadata:
  name: {{ include "quotation-api.fullname" . }}
  labels:
    app: {{ include "quotation-api.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "quotation-api.name" . }}